doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title PNT DECOR | Đăng ký thành viên
    // Fonts & Bootstrap
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&family=Playfair+Display:wght@700&display=swap")
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    
    style.
      :root {
        --primary-gold: #c5a059;
        --dark-space: #121212;
      }

      body, html {
        height: 100%;
        margin: 0;
        font-family: 'Montserrat', sans-serif;
        background-color: #fcfcfc;
      }

      .split-container {
        display: flex;
        min-height: 100vh;
        overflow: hidden;
      }

      /* Bên trái: Hình ảnh cảm hứng nội thất */
      .side-image {
        flex: 1;
        background-image: url('https://images.unsplash.com/photo-1618221195710-dd6b41faaea6?q=80&w=2000');
        background-size: cover;
        background-position: center;
        position: relative;
        display: none;
      }

      .side-image::after {
        content: "";
        position: absolute;
        top: 0; left: 0; right: 0; bottom: 0;
        background: rgba(0, 0, 0, 0.3);
      }

      @media (min-width: 992px) {
        .side-image { display: block; }
        .side-form { width: 500px; }
      }

      /* Bên phải: Form đăng ký */
      .side-form {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        background: white;
        padding: 40px;
        animation: fadeIn 0.8s ease-in-out;
      }

      .register-box {
        width: 100%;
        max-width: 380px;
      }

      .brand-title {
        font-family: 'Playfair Display', serif;
        font-size: 2.2rem;
        color: var(--dark-space);
        margin-bottom: 0;
      }

      .accent-line {
        width: 40px;
        height: 3px;
        background: var(--primary-gold);
        margin: 15px 0 30px 0;
      }

      .form-label {
        font-size: 0.7rem;
        font-weight: 600;
        letter-spacing: 1.2px;
        color: #999;
        text-transform: uppercase;
      }

      .form-control {
        border: none;
        border-bottom: 1px solid #eee;
        border-radius: 0;
        padding: 10px 0;
        font-size: 0.9rem;
        transition: all 0.3s;
      }

      .form-control:focus {
        box-shadow: none;
        border-bottom-color: var(--primary-gold);
      }

      .btn-register {
        background: var(--dark-space);
        color: white;
        border: none;
        border-radius: 0;
        padding: 15px;
        font-weight: 600;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-top: 25px;
        transition: 0.3s;
      }

      .btn-register:hover {
        background: var(--primary-gold);
        color: white;
        transform: translateY(-2px);
      }

      .login-link {
        color: var(--primary-gold);
        text-decoration: none;
        font-weight: 600;
      }

      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }

  body
    .split-container
      // Khối hình ảnh
      .side-image
        .p-5.position-relative(style="z-index: 2; color: white; margin-top: 15%;")
          h2.display-4.fw-bold TRẢI NGHIỆM
          p.fs-5.fw-light Tinh hoa trong từng đường nét thiết kế.

      // Khối Form
      .side-form
        .register-box
          header.mb-4
            h1.brand-title PNT DECOR
            .accent-line
            h3.fw-bold Đăng ký
            p.text-muted.small Tạo tài khoản để nhận tư vấn không gian miễn phí.

          form(action="/user/register" method="POST")
            .mb-4
              label.form-label Họ và tên
              input#fullName.form-control(
                type="text" 
                name="fullName" 
                placeholder="Nhập họ và tên" 
                required
              )
            
            .mb-4
              label.form-label Email
              input#email.form-control(
                type="email" 
                name="email" 
                placeholder="email@vidu.com" 
                required
              )
            
            .mb-4
              label.form-label Mật khẩu
              input#password.form-control(
                type="password" 
                name="password" 
                placeholder="Tối thiểu 8 ký tự"
                required
              )

            .form-check.mb-4
              input#terms.form-check-input(type="checkbox" required)
              label.form-check-label.small.text-muted(for="terms") 
                | Tôi đồng ý với 
                a.text-dark(href="#") Điều khoản dịch vụ

            .d-grid
              button.btn.btn-register(type="submit") Khởi tạo tài khoản
          
          .text-center.mt-5
            p.small.text-muted Bạn đã có tài khoản? 
              a.login-link.ms-1(href="/user/login") ĐĂNG NHẬP NGAY

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")