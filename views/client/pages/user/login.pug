doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title PNT DECOR | Đăng nhập hệ thống
    // Google Fonts: Montserrat cho hiện đại, Playfair Display cho sang trọng
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&family=Playfair+Display:wght@700&display=swap")
    // Bootstrap 5
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    
    style.
      :root {
        --primary-gold: #c5a059;
        --dark-space: #121212;
        --glass-bg: rgba(255, 255, 255, 0.95);
      }

      body, html {
        height: 100%;
        margin: 0;
        font-family: 'Montserrat', sans-serif;
        background-color: #fcfcfc;
      }

      .split-container {
        display: flex;
        min-height: 100vh;
        overflow: hidden;
      }

      /* Bên trái: Hình ảnh thương hiệu */
      .side-image {
        flex: 1;
        background-image: url('https://images.unsplash.com/photo-1600585154340-be6161a56a0c?q=80&w=2070');
        background-size: cover;
        background-position: center;
        position: relative;
        display: none; /* Ẩn trên mobile */
      }

      .side-image::after {
        content: "";
        position: absolute;
        top: 0; left: 0; right: 0; bottom: 0;
        background: linear-gradient(45deg, rgba(18,18,18,0.4), transparent);
      }

      .image-content {
        position: absolute;
        bottom: 10%;
        left: 10%;
        z-index: 2;
        color: white;
        animation: fadeIn 1.2s ease-out;
      }

      /* Bên phải: Form đăng nhập */
      .side-form {
        width: 100%;
        max-width: 500px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: white;
        padding: 40px;
        position: relative;
      }

      @media (min-width: 992px) {
        .side-image { display: block; }
        .side-form { width: 450px; }
      }

      .login-box {
        width: 100%;
        max-width: 360px;
        animation: slideInRight 0.8s ease-out;
      }

      .brand-title {
        font-family: 'Playfair Display', serif;
        font-size: 2.5rem;
        color: var(--dark-space);
        letter-spacing: 2px;
        margin-bottom: 5px;
      }

      .accent-line {
        width: 40px;
        height: 3px;
        background: var(--primary-gold);
        margin-bottom: 30px;
      }

      .form-label {
        font-size: 0.7rem;
        font-weight: 600;
        letter-spacing: 1.5px;
        color: #888;
        text-transform: uppercase;
      }

      .form-control {
        border: none;
        border-bottom: 1px solid #ddd;
        border-radius: 0;
        padding: 12px 0;
        font-size: 0.95rem;
        transition: all 0.3s;
      }

      .form-control:focus {
        box-shadow: none;
        border-bottom-color: var(--primary-gold);
      }

      .btn-login {
        background: var(--dark-space);
        color: white;
        border: none;
        border-radius: 0;
        padding: 15px;
        font-weight: 600;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-top: 30px;
        position: relative;
        overflow: hidden;
        transition: 0.3s;
      }

      .btn-login:hover {
        background: var(--primary-gold);
        color: white;
        transform: translateY(-3px);
      }

      .register-link {
        color: var(--primary-gold);
        text-decoration: none;
        font-weight: 600;
      }

      /* Animations */
      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }

      @keyframes slideInRight {
        from { opacity: 0; transform: translateX(50px); }
        to { opacity: 1; transform: translateX(0); }
      }

  body
    .split-container
      // Khối hình ảnh
      .side-image
        .image-content
          h1.display-3.fw-bold PNT DECOR
          p.lead.fw-light Kiến tạo không gian sống thượng lưu.

      // Khối Form
      .side-form
        .login-box
          .mb-5
            h2.brand-title PNT DECOR
            .accent-line
            h4.fw-bold.text-dark Đăng nhập
            p.text-muted.small Chào mừng bạn quay trở lại.

          form(action="/user/login" method="POST")
            .mb-4
              label.form-label Email
              input#email.form-control(
                type="email" 
                name="email" 
                placeholder="email@pntdecor.vn" 
                required
              )
            
            .mb-4
              label.form-label Mật khẩu
              input#password.form-control(
                type="password" 
                name="password" 
                placeholder="••••••••"
                required
              )

            .d-flex.justify-content-between.align-items-center.mt-2
              .form-check
                input#remember.form-check-input(type="checkbox")
                label.form-check-label.small.text-muted(for="remember") Ghi nhớ
              a.small.text-decoration-none(href="/user/password/forgot" style="color: #bbb") Quên mật khẩu?

            .d-grid
              button.btn.btn-login(type="submit") Đăng nhập hệ thống
          
          .text-center.mt-5
            p.small.text-muted Bạn chưa có tài khoản? 
              br
              a.register-link(href="/user/register") ĐĂNG KÝ THÀNH VIÊN

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")