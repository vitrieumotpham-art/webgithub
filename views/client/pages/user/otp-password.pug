doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title PNT DECOR | Xác thực OTP
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&family=Playfair+Display:wght@700&display=swap")
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    
    style.
      :root {
        --primary-gold: #c5a059;
        --dark-space: #121212;
      }

      body, html {
        height: 100%;
        margin: 0;
        font-family: 'Montserrat', sans-serif;
        background-color: #fcfcfc;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .otp-container {
        width: 100%;
        max-width: 450px;
        padding: 20px;
        animation: fadeIn 0.6s ease-out;
      }

      .otp-card {
        background: white;
        border: none;
        border-radius: 0;
        box-shadow: 0 30px 60px rgba(0,0,0,0.08);
        padding: 50px 40px;
        position: relative;
      }

      .otp-card::before {
        content: "";
        position: absolute;
        top: 0; left: 0; width: 100%; height: 5px;
        background: var(--primary-gold);
      }

      .brand-title {
        font-family: 'Playfair Display', serif;
        font-size: 1.8rem;
        color: var(--dark-space);
        letter-spacing: 3px;
      }

      /* Tùy chỉnh ô nhập mã OTP */
      .otp-input-field {
        letter-spacing: 15px;
        font-size: 2rem;
        font-weight: 700;
        text-align: center;
        border: none;
        border-bottom: 2px solid #eee;
        border-radius: 0;
        color: var(--dark-space);
        transition: all 0.3s;
        padding-left: 15px;
      }

      .otp-input-field:focus {
        box-shadow: none;
        border-bottom-color: var(--primary-gold);
        background: rgba(197, 160, 89, 0.02);
      }

      .btn-confirm {
        background: var(--dark-space);
        color: white;
        border: none;
        border-radius: 0;
        padding: 15px;
        font-weight: 600;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-top: 30px;
        transition: 0.4s;
      }

      .btn-confirm:hover {
        background: var(--primary-gold);
        transform: translateY(-3px);
      }

      .action-links a {
        color: #999;
        text-decoration: none;
        font-size: 0.8rem;
        transition: 0.3s;
      }

      .action-links a:hover {
        color: var(--primary-gold);
      }

      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }

  body
    .otp-container
      .otp-card
        header.text-center.mb-5
          h1.brand-title PNT DECOR
          h4.fw-bold.mt-4 XÁC THỰC OTP
          p.text-muted.small.mt-3 
            | Mã đã gửi tới 
            strong.text-dark #{email}
            br
            | Vui lòng kiểm tra hộp thư của bạn.

        form(action="/user/password/otp" method="POST")
          input(type="hidden" name="email" value=email)
          
          .mb-4
            label.form-label.small.text-uppercase.fw-bold.text-muted(style="letter-spacing:1px") Mã xác thực (6 số)
            input#otp.form-control.otp-input-field(
              type="text" 
              name="otp" 
              placeholder="000000"
              required 
              maxlength="6" 
              pattern="[0-9]{6}"
              autocomplete="one-time-code"
            )
          
          .d-grid
            button.btn.btn-confirm(type="submit") Xác nhận mã
        
        footer.text-center.mt-5.action-links
          .d-flex.justify-content-center.gap-3
            a(href="/user/password/forgot") Gửi lại mã
            span.text-light |
            a(href="/user/login") Quay lại Đăng nhập

    script.
      // Tự động focus vào ô nhập OTP khi load trang
      document.addEventListener('DOMContentLoaded', function() {
        document.getElementById('otp').focus();
      });