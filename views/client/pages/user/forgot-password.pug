doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title PNT DECOR | Khôi phục mật khẩu
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&family=Playfair+Display:wght@700&display=swap")
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    
    style.
      :root {
        --primary-gold: #c5a059;
        --dark-space: #121212;
      }

      body, html {
        height: 100%;
        margin: 0;
        font-family: 'Montserrat', sans-serif;
        background-color: #fcfcfc;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      /* Khung chứa trung tâm */
      .forgot-container {
        width: 100%;
        max-width: 450px;
        padding: 20px;
        animation: fadeIn 0.8s ease-in-out;
      }

      .forgot-card {
        background: white;
        border: none;
        border-radius: 0; /* Vuông vức theo phong cách kiến trúc */
        box-shadow: 0 30px 60px rgba(0,0,0,0.08);
        padding: 50px 40px;
        position: relative;
        overflow: hidden;
      }

      /* Điểm nhấn vàng đồng phía trên */
      .forgot-card::before {
        content: "";
        position: absolute;
        top: 0; left: 0; width: 100%; height: 5px;
        background: var(--primary-gold);
      }

      .brand-title {
        font-family: 'Playfair Display', serif;
        font-size: 1.8rem;
        color: var(--dark-space);
        text-align: center;
        margin-bottom: 30px;
        letter-spacing: 3px;
      }

      .form-label {
        font-size: 0.7rem;
        font-weight: 600;
        letter-spacing: 1.2px;
        color: #999;
        text-transform: uppercase;
      }

      .form-control {
        border: none;
        border-bottom: 1px solid #eee;
        border-radius: 0;
        padding: 12px 0;
        font-size: 0.95rem;
        transition: all 0.3s;
      }

      .form-control:focus {
        box-shadow: none;
        border-bottom-color: var(--primary-gold);
      }

      .btn-send {
        background: var(--dark-space);
        color: white;
        border: none;
        border-radius: 0;
        padding: 15px;
        font-weight: 600;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-top: 30px;
        transition: 0.4s;
      }

      .btn-send:hover {
        background: var(--primary-gold);
        transform: translateY(-3px);
      }

      .back-link {
        color: #bbb;
        text-decoration: none;
        font-size: 0.8rem;
        letter-spacing: 1px;
        transition: 0.3s;
        display: inline-flex;
        align-items: center;
        gap: 8px;
      }

      .back-link:hover {
        color: var(--primary-gold);
      }

      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }

  body
    .forgot-container
      .forgot-card
        header.text-center.mb-5
          h1.brand-title PNT DECOR
          h4.fw-bold.mt-4 QUÊN MẬT KHẨU
          p.text-muted.small.mt-2 Nhập email để khôi phục quyền truy cập không gian của bạn.

        form(action="/user/password/forgot" method="POST")
          .mb-4
            label.form-label EMAIL CỦA BẠN
            input#email.form-control(
              type="email" 
              name="email" 
              placeholder="nhap-email@example.com" 
              required
            )
          
          .d-grid
            button.btn.btn-send(type="submit") Gửi mã xác thực
        
        footer.text-center.mt-5
          a.back-link(href="/user/login") 
            span ←
            span QUAY LẠI ĐĂNG NHẬP

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")